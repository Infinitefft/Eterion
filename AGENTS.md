# Eterion 项目协作原则

## 核心原则：避免过度设计

这是一个用于大厂秋招展示的个人项目，不是生产级商业系统。方案设计和代码实现应保持适中的复杂度：不能只是玩具，需要具备一定的技术含量、工程完整性和可讲述性；但不要为了“看起来高级”而引入没有实际收益的复杂架构、抽象或基础设施。

- 我的主要求职方向是前端开发工程师。时间和精力应优先投入前端体验、交互、工程化、性能和能体现前端能力的技术亮点。
- 后端与 Agent 能力是加分项，应做到完整、合理、可演示，但不必追求生产级的复杂度。
- 默认选择简单、清晰、可靠、易维护的实现，先完成最小但充分的方案，再根据明确需求迭代。
- 不需要穷举低概率的极端边界情况，也不要提前设计暂时用不到的扩展能力。
- 避免无明确收益的微服务拆分、过深抽象、复杂设计模式、过重基础设施和过度封装。
- 引入额外复杂度前，先判断它能否明显提升秋招展示价值、用户体验、核心能力或项目可信度；如果不能，就采用更简单的方案。
- 做技术取舍时，优先考虑：能否稳定运行、能否清楚解释、能否体现个人贡献，以及面试时是否有值得展开的技术点。

除非我明确要求生产级方案，否则请始终以上述定位控制实现范围，并主动提醒可能出现的过度设计。

## 项目展示主线

项目应围绕“高性能 AI 对话前端 + 具备完整 Agent 能力的应用”形成连贯主线，而不是堆砌彼此无关的技术名词。以下内容是计划中的重点；尚未完成的能力必须明确标记为计划，不能描述成已经实现。

### 前端重点

- 基于 WebSocket 设计面向 AI 对话场景的 IM 通信协议，重点体现消息模型、状态流转、流式传输、中断与必要的重连处理，以及前端侧的整体架构设计。
- 使用“稳定块 + 活动块”和自研轻量 Markdown 解析器实现高性能流式渲染，核心目标是减少重复解析、重复渲染和流式输出时的页面抖动。
- 计划为长聊天记录加入虚拟列表，重点处理动态高度、滚动锚定以及虚拟列表与流式消息更新的配合。
- 已使用 Access Token + Refresh Token 的双 Token 机制实现前端无感刷新和会话续期。该能力可以体现前端请求链路与认证状态管理，但是否作为核心亮点，应取决于是否还处理了并发刷新、失败收敛和请求恢复等具有实际价值的问题，不能补写尚未实现的机制。

### Agent 重点

- 实现 Tools 和 Skills，并根据实际需求逐步加入 RAG 与分层记忆。
- 建立轻量但可信的 Agent 评估流程，覆盖典型任务集以及工具选择、参数正确性、任务完成质量、延迟和成本等关键维度。目标是证明 Agent 的效果并支持迭代，而不是建设复杂的评估平台。
- RAG 计划覆盖切分、Embedding、检索、语义鸿沟处理、重排和多查询等关键流程；每个环节都应有明确问题和效果依据，不能只为完整术语链路而加入。
- 记忆体系计划分为三层。设计时重点讲清每层负责什么、何时写入和读取、如何更新或淘汰，以及如何避免错误记忆污染后续回答。

### 后端定位

后端是加分项和项目支撑，不是主要展示方向。优先把数据库、并发和缓存思想应用到真实需求，例如 WebSocket 连接与消息处理、消息持久化与索引、必要的并发安全和确有收益的缓存。不要为了展示后端技术而额外引入微服务、消息队列、分布式架构或没有真实收益的缓存层。

## 我的参与边界

- 对简历中的核心亮点和难点，我需要深度参与设计、实现与复盘，尤其是 WebSocket IM 协议、流式 Markdown 渲染、虚拟列表、Agent 评估、RAG 和记忆体系。
- 处理这些核心内容时，不要只给出黑盒式成品。应同步讲清问题背景、数据流、关键方案、取舍、失败风险和验证方法，并将实现拆成便于理解和 Review 的改动。
- 通用页面、样板代码、常规 CRUD、配置、重复性类型定义、普通接口接入、基础测试和文档整理等非核心工作，可以由 Agent 直接完成，我只做必要 Review。
- 如果某项工作会显著增加复杂度，应指出它对项目展示或真实体验的具体收益；收益不明确时，默认不做。

## 亮点的完成标准

- 一个能力只有在“实际可运行、能够演示、原理可解释、效果有证据”时，才适合作为简历亮点。
- 性能优化应尽量先建立基线，再记录优化前后的数据。流式 Markdown 和虚拟列表至少应有贴近真实聊天场景的性能对比，而不是只凭主观感受宣称高性能。
- Agent、RAG 和记忆能力应使用固定案例或小型评估集验证，记录成功案例、失败案例与已知限制，避免只展示一次效果较好的调用。
- 实现核心亮点时，保留简短的设计说明、关键取舍和验证结果，方便后续整理简历以及准备面试回答。
- 不为了增加简历关键词虚构复杂度，也不把计划中的功能写成已经落地的成果。
